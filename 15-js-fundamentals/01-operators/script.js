// ! Arithmetic Operators
// Addition Operator(+) adds two numbers together
const applesOnHand=5;
const applesReceived=10;
const totalApples=applesOnHand+applesReceived;
// Subtraction Operator(-) subtracts two numbers together
const totalTickets=100;
const ticketsSold=45;
const ticketsLeft=totalTickets-ticketsSold;
// Multiplication Operator(*) multiplies two numbers together
const bookCost=120;
const booksBought=15;
const bookCost=bookCost*booksBought;
// Division Operator(/) divides two numbers together
const totalPages=200;
const pagesPerDay=40;
const daysToFinish=totalPages/pagesPerDay;
// Remainder, modulo Operator(%) divides one number by another and returns the remainder
const peopleParty=10;
const pizzasAtParty=3;
const sliceLeftOver=peopleParty/pizzasAtParty;
// Increment Operator(++) increases a number by 1
let currentWebsiteViews=1000;
currentWebsiteViews++;
// Decrement Operator(--) decreases a number by 1
let basketBallPoints=1000;
basketBallPoints--;

// !Assignment Operators
// Assignment Operator(=)
// Assigns a value to a variable.
let mytotalApples=10;
// Addition Assignment Operator(+=)
// The addition assignment operator adds the value on the right to the variable on the left & then assigns the result to the variable on the left.
let population=100;
population+=50;
// Subtraction Assignment Operator(-=)
// The subtraction assignment operator subtracts the value on the right to the variable on the left & then assigns the result to the variable on the left.
let moneyInWallet=200;
moneyInWallet-=30;
// Multiplication Assignment Operator(*=)
let cookiesInJar=12;
cookiesInJar*=3;
// Division Assignment Operator(/=)
let myTotalPages=200;
myTotalPages/=5;
// Remainder/Modulo Assignment Operator(%=)
let tickets=25;
tickets%=4;
// Exponentiation assignment Operator(**=)
// Find out 2 to the raise to the power of 4
let base=2;
base**=4;

// ! Comparison Operators
// Equal to(== or ===)
// Checks is the values of two operands are equal or not, regardless of their type.
// Double eqaulto(==) only checks the value without taking the datatype into consideration
// But Triple equalto(===) takes the data type & value.
let heightA='150';
let heightB=150;
const results=heightA==heightB;
// Not Equal to(!=)
// Checks if the values of two operands are different, regardless of their datatypes.
let apples=5;
let oranges=7;
const result=apples!=oranges;
// Not Equal Value or Not Equal Type(!==)
// Checks if the values and/or types of two operands are not identical.
let resultHeight=heightA!==heightB;
// Greater than (>)
let speedA=60;
let speedB=50;
let resultSpeed=speedA>speedB;
// Less than (<)
let speedY=60;
let speedZ=50;
let resultSpeedX=speedY<speedZ;
// Greater than or equal to(>=)
// checks if the value of the left operand is greater than or equal to the value of the right operand.
let weightA=50;
let weightB=50;
let resultWeight=weightA>=weightB;
// Less than or equal to(<=)

// ! Logical Operators
// These are used to combine conditional statements & evaluate multiple conditions in order to determine the final outcome.
// Logical AND(&&)
// Returns true if both operands are true. Otherwise, it returns false.
let isSunny=true;
let isWarm=true;
let shouldGoToPark=isSunny&&isWarm;
// Logical OR(||)
// Returns true if either or both of the operands are true. It returns false only if both operands are false.
let isRain=false;
let isSnowy=true;
let isNight=flase;
const resultOutside=isRain||isSnowy||isNight;

 
